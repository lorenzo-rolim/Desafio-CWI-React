import React from 'react';
import P from 'prop-types';
import * as Styled from './styles';

const DatailsSection = ({ children }) => {
  return (
    //
    <Styled.Container></Styled.Container>
  );
};

DatailsSection.propTypes = {
  children: P.node,
};

export default DatailsSection;
