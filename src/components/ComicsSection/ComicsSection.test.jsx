import ComicsSection from '.';
import { renderTheme } from '../../styles/render-theme';

describe('<ComicsSection />', () => {
  it('should render', () => {
    renderTheme(<ComicsSection></ComicsSection>);
  });
});
